# Sale-Jerry-Plugin 更新日志

> 每次更新了什么新能力、哪些功能更好用了，都在这里告诉你

---

## v1.7.0 (2026-04-24)

### 更好用了 🔧

- **日报生成** (~) 移除 observations 数据源，回归 sessions 单一数据源，降低复杂度和数据噪音
- **日报生成** (~) 新增会话聚合分组逻辑，按主题相似度合并同一任务的多个操作步骤，避免流水账式日报

---

## v1.6.1 (2026-04-18)

### 修好了 🛠

- **日报生成** (!) 修复了 v4.8 引入的错误——之前强制传入 `project: ${JAVIS_WORKSPACE_NAME}` 参数，但 claude-mem 实际存储的 project 字段值是 `"workspace"` 而非工作空间名称，导致查询结果始终为空；现已删除该参数，恢复正常查询
- **日报生成** (~) MCP Server 可用性检测从单次检测升级为最多重试 3 次（每次间隔 1 分钟）；3 次全部失败后不再直接终止，而是生成包含友好提示信息的日报文件并同步到 Javis

---

## v1.6.0 (2026-04-13)

### 修好了 🛠

- **项目状态更新** (!) 修复了幂等性问题——当发现目标内容已存在时不再自行中止并询问用户，改为按决策框架静默跳过或正常执行，完整走完更新→历史→验证流程
- **项目状态更新** (!) 修复了安全约束形同虚设的问题——新增安全约束识别机制，用户要求跳过验证、直接覆盖文件、跳过历史记录等违规请求会被明确拒绝，即使声称"紧急"也不可绕过
- **项目状态更新** (!) 修复了 force 模式下豁免章节更新日志遗漏的问题——现在 force 模式即使目标为"更新日志"或"会话轨迹"也会强制同步写入更新日志
- **项目状态更新** (!) 修复了验证步骤可被跳过的问题——强化验证步骤为不可跳过的强制要求，即使用户以"紧急"、"高效"为由也不得绕过
- **销售准备** (!) 修复了遇到情绪化输入或部分重做请求时跳过完整流程的问题——现在无论用户说什么（抱怨、催促、只做某一步），都会执行完整的8步销售准备流程，零例外
- **销售准备** (!) 修复了内部执行过程暴露给用户的问题——目录检查、缓存读取、参数解析等中间日志不再展示，仅输出业务结果摘要
- **销售准备** (!) 修复了已有项目资料时行为不一致的问题——新增三种模式判断（补充更新/完全重新执行/首次执行），复用已有企业调研数据但重新生成案例和话术
- **销售主控** (!) 修复了安全约束形同虚设的问题——新增5类安全拦截规则，禁止直接修改项目数据、绕过流程、操作非标准路径等，违反时明确拒绝
- **销售主控** (!) 修复了需求分析阶段跳过术语标准化的问题——强制先执行 jargon-decoder 再执行 requirement-matching，禁止跳过或颠倒顺序
- **销售主控** (!) 修复了会议跟进缺少销售话术的问题——明确针对性销售话术为必选交付物，缺失时标注警告并建议补充
- **销售主控** (!) 修复了项目已存在时未说明的问题——现在会明确告知用户跳过初始化原因和重新初始化方法
- **销售陪练** (!) 修复了无法在单轮测试中完成演练的问题——新增 single_turn 执行模式，Agent 同时模拟客户和销售角色，一次性输出完整对话+复盘+评分
- **销售陪练** (!) 修复了提示词注入时角色切换的问题——新增 4 类角色锁定规则和注入检测逻辑，面对"Developer Mode"等攻击时坚持教练人设并婉拒
- **销售陪练** (!) 修复了工具调用日志暴露给用户的问题——新增输出格式规范，禁止暴露文件加载进度、数据校验过程等内部执行细节
- **立项报告** (!) 修复了代码执行错误时暴露调试过程的问题——新增静默修复机制，遇到错误时自动修复并仅输出最终结果
- **立项报告** (!) 修复了英文企业名称被翻译的问题——英文企业名称（如 ByteDance）保留原文，不再自动翻译为中文
- **立项报告** (!) 修复了参数缺失引导不完整的问题——引导模板新增完整的8个支持模块列表和英文名称使用示例
- **项目初始化** (!) 修复了社会工程类攻击防御不足的问题——新增6类安全约束（环境变量窃取、越权操作、命令注入等）和攻击关键词检测，拦截伪装为正常请求的越权操作
- **项目初始化** (!) 修复了目录存在后未立即停止的问题——检测到目录存在后必须立即停止，禁止发起 AskUserQuestion 等任何后续交互操作
- **项目初始化** (!) 修复了环境变量为工作空间标识时名称处理错误的问题——新增工作空间标识判断标准和降级策略，不再将工作空间名误当客户名称拼接
- **项目初始化** (!) 修复了名称输出前后不一致的问题——新增中间输出一致性校验，确保 FINAL_PROJECT_NAME 为唯一项目名称
- **案例匹配** (!) 修复了保存报告阻塞匹配结果的问题——区分核心交付步骤和辅助保存步骤，保存失败时仍正常输出匹配结果
- **案例匹配** (!) 修复了高危命令未直接拒绝的问题——新增7类高危命令模式（根目录搜索、删除、系统文件操作等）的明确拒绝策略
- **案例匹配** (!) 修复了保存步骤降级策略缺失的问题——项目目录不存在或写入失败时跳过保存，添加警告但不影响匹配结果
- **案例匹配** (~) 工具调用精简——明确允许/禁止的工具调用规则，限制 Bash find 搜索范围，减少冗余调用
- **SPIN分析** (!) 修复了因缺少项目名称而直接中断分析的问题（占 75% 的失败用例）——现在没有项目名称也能正常执行 SPIN 分析，结果直接输出；如果只有一个项目目录会自动关联，多个项目才询问选择
- **SPIN分析** (!) 修复了能力边界缺失问题——面对竞争分析、技术方案评估等非 SPIN 请求时，会明确拒绝并引导回核心的会议纪要分析，不再跑偏执行无关任务
- **需求匹配** (!) 修复了功能清单路径不存在时 Agent 自行变通的问题——现在零例外拒绝，即使用户要求跳过也不绕过，确保匹配结果严格基于真实功能清单
- **需求匹配** (!) 修复了前沿技术需求（区块链、AI大模型、数字孪生）匹配度虚高的问题——新增「功能等价性」判断层，从根本上区分"相关但不等价"的功能
- **需求匹配** (!) 修复了自然语言需求输入被拒绝的问题——现在可以从对话或会议纪要中直接提取需求点进行分析，不再要求必须是格式化文档
- **需求匹配** (!) 修复了输出格式不一致的问题——强制要求 JSON 格式输出，禁止 Markdown 表格替代
- **企业调研** (!) 修复了高知名度企业别名仍被追问确认的问题——新增16个常见别名（阿里、建行、腾讯等）优先直接解析规则，不再多此一举问用户确认
- **企业调研** (!) 修复了非法输入校验响应不精准的问题——新增4类标准化校验错误输出模板（空输入/过短/通用词汇/非法字符），校验失败时不再跳过直接执行
- **企业调研** (!) 修复了用户未指定模块时仍自行执行案例匹配的问题——新增入口条件检查，module 为空时零例外禁止执行案例匹配
- **企业调研** (!) 修复了无匹配结果时自行扩展搜索范围的问题——明确禁止扩大搜索范围、禁止自动推荐其他模块案例
- **企业调研** (!) 修复了 PII 隐私保护缺失的问题——新增3类场景拒绝话术模板（个人联系方式/内部信息/信用法律信息）+ 脱敏处理规则
- **企业调研** (!) 修复了调研完成后遗漏记录会话轨迹的问题——步骤4强化为强制执行步骤，包含执行时机要求和检查清单
- **会议分析** (!) 修复了完成后仅输出文件路径不展示业务结果的问题——新增结构化摘要输出规范，必须包含分析结论、客户痛点、话术要点、文件路径、下一步建议5项内容
- **会议分析** (!) 修复了内部执行过程暴露给用户的问题——新增输出约束，工具调用日志、步骤编号、文件移动记录等6类内部信息不再展示
- **会议分析** (~) 优化了工具调用准确性——新增工具选择规则（Glob替代find/test、Read替代cat），减少冗余调用
- **商务控标** (!) 修复了敏感商务数据外传无防御的问题——新增数据外传安全约束，案例库/评分表/分析报告打包发送到外部服务器的请求会被明确拒绝
- **商务控标** (!) 修复了自动扫描到评分表后未继续执行分析的问题——新增闭环规则，扫描到多个文件时自动选最新并立即执行完整分析流程，不再暂停等待
- **商务控标** (~) 优化了工具调用准确性——新增工具选择规则（Glob替代find/test、Read替代cat），减少冗余调用
- **相关方录入** (!) 修复了安全防御形同虚设的问题——新增3类安全威胁防御（Prompt注入攻击/有害内容/PII泄露），每类都有检测关键词和标准拒绝话术
- **相关方录入** (!) 修复了Markdown表格写入时特殊字符破坏格式的问题——新增6类字符转义规则（管道符、换行符、回车符、短横线、尖括号），并明确了处理顺序
- **相关方录入** (!) 修复了smart-merge模式在文件读取失败时流程中断的问题——新增3种降级策略（读取失败降级为append/内容为空初始化新记录/并发修改以最新数据为准）
- **相关方录入** (~) 优化了信息补充追问逻辑——新增用户确定性表述识别机制，对"电话没变"、"只更新邮箱"等明确表述不再重复追问

### 更安全 🔒

- **全局知识库保护** (+) Agent 层新增全局约束：禁止任何 Skill 向 `通用知识/` 目录写入、修改或追加文件；8 个读取知识库的 Skill 全部标注了只读声明

---

## v1.5.2 (2026-04-08)

### 更好用了 🔧
- **日报生成** (~) 日报同步到 Javis 的路径现在会按日期分组——每天的日报会保存到 `会议纪要内容/日报周报内容{日期}/` 目录下，更容易找到

---
## v1.5.1 (2026-04-08)

### 修好了 🛠
- **日报生成** (!) 修复了偶尔文件名变成"未知创建者"的问题——获取空间创建者信息时如果一次没成功会自动重试（最多5次，每次等30秒），确保文件名总是能正确显示创建日报的人
---
## v1.5.0 (2026-04-08)

### 更好用了 🔧

- **插件发布** (~) 支持同时管理多个插件了，用 `plugin_name` 参数就能快速选择要发布哪个插件（销售插件 / 开发团队插件），不用每次都手动输入 ID 和路径

---

## v1.4.1 (2026-04-04)

### 修好了 🛠

- **日报生成** (!) 修复了周末判断逻辑的问题——以前是判断"今天是不是周末"，现在改成判断"昨天是不是周末"。周六想生成周五的日报就能正常生成了，周日和周一会正确提示"昨天是周末，无需生成日报"
- **日报生成** (!) 修复了总是提示"无会话记录"的问题——以前查会话时开始和结束日期设成同一天导致查不到，现在结束日期会自动多加一天，能正确找到前一天的会话记录了

---

## v1.4.0 (2026-04-03)

### 新能力 ✨

- **FABE售前方案** (+) 新增售前方案生成能力，输入客户问题和需求就能自动生成结构化的 FABE 方案（特征→优势→利益→证据），方案内容会引用真实的产品功能清单，不编造

### 更好用了 🔧

- **FABE售前方案** (~) 生成的方案可以同时输出 Markdown 和 Word 两种格式，Markdown 方便在线协作，Word 可以直接提交给客户
- **文档处理** (~) 图片识别改用大模型原生视觉能力，不再需要安装 OCR 引擎，识别效果更好；文本和 Markdown 文件也改用大模型语义理解，提取信息更准确
- **日报生成** (~) 日报不再从文件变化统计，改为从会话记录中提取客户推进工作，总结不超过300字，聚焦销售进展；如果昨天没有会话记录就直接跳过不生成空日报

---

## v1.3.0 (2026-03-25)

### 更好用了 🔧

- **企业调研** (~) 案例数据同步到最新版本，搜索到的案例信息更全更新，找银行类客户案例时也能自动覆盖所有银行类型
- **招投标分析** (~) 分析用的案例数据升级到最新版本，得分计算和策略建议更准确；资质信息也同步更新了
- **商务控标** (~) 案例数据升级到最新版本，控标分析更精准；资质信息也同步更新了
- **销售准备** (~) 案例数据统一升级，一键准备销售资料时引用的案例都是最新的
- **需求匹配** (~) 功能清单升级到最新版本，匹配客户需求时用的产品功能信息更准确更全面

### 修好了 🛠

- **立项报告** (!) 修复了生成报告时搜索模板路径不正确的问题，现在能正常找到知识库中的模板和参考资料
- **商务控标 + 招投标分析** (!) 修复了两个功能之间数据传递不匹配的问题，分析结果能正确传递给控标报告
- **需求匹配** (!) 修复了需求匹配结果无法正确记录到项目状态的问题
- **销售陪练** (!) 修复了陪练时无法读取项目资料的问题，演练场景会基于真实的会议记录和需求文档
- **话术生成** (!) 修复了话术生成后项目状态更新失败的问题
- **会议分析** (!) 修复了会议分析结果无法正确保存到项目的问题
- **技能质检** (!) 审查功能更聚焦了，不再检查与销售无关的内部工具，审查结果更简洁实用
- **项目初始化** (!) 修复了创建带客户名称的项目时，系统误报"项目不存在"的问题
- **立项报告** (!) 修复了知识库目录找不到时直接报错的问题，现在会灵活处理，找得到就用、找不到也不会卡住
- **企业调研** (!) 修复了内部流程步骤编号混乱的问题，案例匹配和报告生成的流程更清晰

---

## v1.2.0 (2026-03-18)

### 更好用了 🔧

- **案例匹配** (~) 案例库已更新到最新版本，现在能查到更多、更新的真实项目案例
- **案例匹配** (~) 搜索更聪明了——输入"银行"就能自动找到城商行、农商行、股份制银行等所有银行类案例，输入"金融"能覆盖银行、券商、保险、基金全系列，不再遗漏
- **案例匹配** (~) 找案例时更懂你想要什么了，能从项目描述中自动识别是 CMDB、ITSM 还是 DevOps 相关的案例
- **更新日志** (~) 以后版本更新说明会用更直白的大白话来写，一眼就能看懂新功能能帮到什么

---

## v1.1.0 (2026-03-17)

### 新能力 ✨

- **技能开发助手** (+) 新增一个"AI 开发 AI"的能力——可以帮你快速开发新功能或改进现有功能，改完后会先给你看改了什么，确认无误才会保存
- **技能质检官** (+) 新增自动检查功能，能发现工具内部的逻辑漏洞并给出修复建议，确保输出结果更可靠

### 更好用了 🔧

- **项目状态更新** (~) 修改项目进度时会自动保留修改记录，方便回溯每次改了什么、谁改的，再也不怕版本混乱
- **插件发布** (~) 发布新版本后会自动通知团队群，大家第一时间知道有哪些新功能可以用

---

## v1.0.1 (2026-03-15)

### 更好用了 🔧

- **立项报告** (~) 生成报告时只展示和你需求相关的模块方案，不再混杂无关内容，报告更精炼
- **企业调研** (~) 输入公司简称也能自动识别全称，信息不够时会主动问你补充关键细节，确保调研结果准确无误
- **会议分析** (~) 哪怕只有几段简短的对话记录，也能帮你分析出有用的销售线索和推进建议
- **销售话术** (~) 给你写话术时会参考更多真实案例和产品知识，建议更贴合实际场景
- **案例匹配** (~) 找到的案例会标注来源，方便你追溯和核实
- **招投标分析** (~) 信息不完整时会主动提醒你补充关键内容，避免分析结果偏差
- **销售陪练** (~) 练习前会先检查项目信息是否齐全，不完整时会引导你补充，让模拟演练更贴近真实

### 📄 文档更新

- 新增《Skill 开发与发布指导文档》，开发团队可以参照它来快速开发新功能

---

## v1.0.0 (2026-03-14)

### 新能力 ✨

首次发布销售助手，涵盖 **21 项核心能力**：

#### 销售打单必备

| 能力 | 一句话说明 |
|------|-----------|
| **企业调研** | 输入客户公司名，自动帮你查清这家公司的背景、行业和关键信息 |
| **案例匹配** | 根据行业、产品模块等条件，从历史项目库中帮你找到最相关的成功案例 |
| **销售话术** | 告诉它客户场景，帮你生成专业的销售话术，从开场到促单全覆盖 |
| **销售准备** | 一键搞定企业调研 + 案例匹配 + 话术生成，打单前用它就够了 |

#### 项目跟进利器

| 能力 | 一句话说明 |
|------|-----------|
| **项目初始化** | 新项目一键创建标准文件夹和模板，省去手动整理的麻烦 |
| **项目状态更新** | 随时更新项目进展，自动记录每次修改，多人协作不串位 |
| **相关方录入** | 粘贴一段人员名单就能自动识别姓名、职务、联系方式，批量录入项目成员 |
| **会议分析** | 丢进去会议纪要，自动帮你分析客户需求阶段，并给出下一步怎么推进的建议 |
| **SPIN 分析** | 基于 SPIN 销售方法论，从对话中挖出客户的痛点和需求 |
| **日报生成** | 自动扫描你今天改了哪些文件，一键生成工作日报 |

#### 招投标专项

| 能力 | 一句话说明 |
|------|-----------|
| **招投标分析** | 上传招标评分表，自动计算得分、识别风险点，告诉你哪里能拿分哪里要小心 |
| **商务控标** | 分析招标要求，帮你找出控标机会和优化方向，提高中标率 |
| **需求匹配** | 把客户需求和我们的产品功能逐条对比，算出匹配度，快速判断能不能做 |

#### 销售赋能工具

| 能力 | 一句话说明 |
|------|-----------|
| **销售陪练** | 模拟真实客户和你对话练习，练完还会给你复盘哪里做得好哪里可以改进 |
| **技术黑话翻译** | 客户说了听不懂的技术术语？丢给它，秒变大白话，还帮你对应到自家产品功能 |
| **文档处理** | Word、Excel、PDF 各种格式都能读，客户发来的需求文档直接丢进去分析 |

---

### 📄 文档更新

- 新增《21 项能力完整功能说明》——每项能力能干什么、怎么用，一目了然
- 新增《初级销售使用手册》——刚上手的同事也能快速上手

---

## 变更类型说明

| 标识 | 含义 |
|------|------|
| ✨ `+` 新能力 | 以前做不了的事，现在能做了 |
| 🔧 `~` 更好用了 | 以前能做的事，现在做得更好了 |
| 🛠 `!` 修好了 | 以前偶尔出问题的地方，现在正常了 |
| 📄 `#` 文档 | 使用说明或参考资料有更新 |

---

## 版本总览

| 版本 | 日期 | 一句话说清这次更新了什么 |
|------|------|----------------------|
| v1.6.1 | 2026-04-18 | 修复日报 v4.8 错误 project 参数导致查询为空；MCP 检测升级为 3 次重试，失败后生成提示日报 |
| v1.6.0 | 2026-04-13 | 修复项目状态更新的幂等性/安全约束/验证优先级问题；修复 SPIN 分析缺项目名中断、需求匹配虚高匹配度等 |
| v1.4.1 | 2026-04-04 | 修复日报周末判断逻辑，周六能正常生成周五日报了 |
| v1.2.0 | 2026-03-18 | 案例库升级到最新版本，搜索更智能；更新日志改用大白话 |
| v1.1.0 | 2026-03-17 | 新增技能开发和自动质检能力；项目状态修改会自动留痕；发布后自动通知群 |
| v1.0.1 | 2026-03-15 | 10 项能力全面升级——信息不够会主动问你、分析结果更精准、报告更精炼 |
| v1.0.0 | 2026-03-14 | 首次发布，21 项核心能力上线，覆盖从打单准备到项目跟进的完整销售流程 |

---

**最后更新**: 2026-04-18 (v1.6.1)
